void printi(int i) {
  int remainder;
  int quotient;

  remainder = 0;
  quotient = i;

  while(quotient > 0) {
      remainder = quotient % 10;
      quotient = quotient / 10;
      putchar(48 + remainder);
  }
}